<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Storage permissions for compatibility with devices running Android 12 (API level 32) and below -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Hardware features -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EssentialsMonitor"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/GOOGLE_MAP_API_KEY" />

        <!-- Splash Activity -->
        <activity
            android:name=".AppStartupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DashboardHomeActivity"
            android:exported="false" />

        <activity
            android:name=".ProductBrowserActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Authentication Activities -->
        <activity
            android:name=".UserAuthenticationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity
            android:name=".UserRegistrationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Product Activities -->
        <activity
            android:name=".ProductListActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ProductDetailActivity"
            android:exported="false"
            android:parentActivityName=".ProductListActivity" />

        <!-- Price and Store Activities -->
        <activity
            android:name=".PriceReportActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ProductDetailActivity" />

        <!-- Map Activity -->
        <activity
            android:name=".MapViewActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Profile Activities -->
        <activity
            android:name=".UserProfileActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".AppSettingsActivity"
            android:exported="false"
            android:parentActivityName=".UserProfileActivity" />

        <activity
            android:name=".ProductCreationActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- GeoSensor Activity -->
        <activity
            android:name=".GeoSensorActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Store Management Activity -->
        <activity
            android:name=".StoreManagementActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Report Analytics Activity -->
        <activity
            android:name=".ReportAnalyticsActivity"
            android:exported="false"
            android:parentActivityName=".DashboardHomeActivity" />

        <!-- Edit Profile Activity -->
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:parentActivityName=".UserProfileActivity" />

    </application>

</manifest>